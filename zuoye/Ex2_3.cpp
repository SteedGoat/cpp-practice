#include <stdio.h>
#include <cmath>
int main()
{
    float x = 100.0, y = 200.0;
    printf("第一个原数为%f\n",x);
    printf("其绝对值为%f\n",fabs(x));
    printf("第二个原数为%f\n",y);
    printf("其绝对值为%f\n",fabs(y));
    return 0;
}